---
import Layout from "../../components/Layout.astro";
import PageHero from "../../components/PageHero.astro";
import QuickFacts from "../../components/QuickFacts.astro";
import { site } from "../../lib/site";
import headerImage from "../../assets/images/control-head-repair-header.webp";

const primaryCta = site.phoneNumber && site.phoneNumber !== "PHONE_NUMBER"
  ? { href: `tel:${site.phoneNumber}`, text: `📞 ${site.phoneNumber}` }
  : { href: "/contact/", text: "📞 Call for a Free Consultation" };

const signs = [
  { name: "Constant Regeneration", detail: "The unit regenerates far more often than its programmed schedule, wasting salt and water on every extra cycle." },
  { name: "No Regeneration at All", detail: "The control head fails to trigger a cycle, letting resin exhaust while hard water passes straight through." },
  { name: "Mistimed Regeneration", detail: "Regeneration fires at an unexpected hour or on an unexpected day, usually pointing to a lost or drifting internal clock." },
  { name: "Display Errors or Blank Screen", detail: "An error code, frozen display, or completely blank screen signals a electronics or power-board fault inside the head." },
  { name: "Motor Grinding or Stuck", detail: "A control head motor that grinds, stalls, or fails to turn the internal piston prevents the valve from cycling correctly." },
];

const faqs = [
  {
    q: "Can a control head be reprogrammed instead of replaced?",
    a: "Yes — a drifting clock, an incorrect hardness setting, or an incorrect regeneration schedule is a reprogramming fix, not a replacement, and takes under 30 minutes on a working head.",
  },
  {
    q: "When does a control head need full replacement?",
    a: "A cracked piston, failed motor, or dead circuit board on a unit over 10 years old is usually cheaper to replace outright than to source parts for, especially on discontinued models such as older Fleck or Autotrol heads.",
  },
  {
    q: `Does control head failure mean the whole softener is done?`,
    a: `No. The resin tank and brine tank typically outlast the control head — a head swap on an otherwise sound system is a common, cost-effective repair in ${site.city}.`,
  },
];
---

<Layout
  title={`Water Softener Control Head Repair in ${site.city}, ${site.stateAbbr}`}
  description={`Signs your water softener's control head is failing, repair vs. replacement, and what a control head repair costs in ${site.city}.`}
>
  <PageHero
    breadcrumbs={[{ label: "Repair", href: "/repair/" }, { label: "Control Head Repair" }]}
    heading={`Control Head Repair in ${site.city}, ${site.stateAbbr} | Trusted Local Specialists`}
    image={headerImage}
    primaryCta={primaryCta}
    secondaryCta={{ href: "/quote/", text: "Get a Free Water Softener Quote in Indianapolis" }}
  >
    <p slot="opening">
      <a href="/" class="brand-link">Water Softeners of {site.city}</a>
      {" "}offers water softener control head repair at {site.gpgLow}-{site.gpgHigh} GPG, such as
      reprogramming, sensor fixes, and motor replacement, across {site.city} and {site.county}. The control
      head manages regeneration cycles. A faulty control valve causes the softener to regenerate continuously,
      not at all, or at irregular intervals, passing hard water through untreated. Free diagnostic included
      with every repair call. We diagnose and service all major control head brands, such as Fleck, Clack, and Autotrol. Same day availability.
    </p>
  </PageHero>

  <section class="section">
    <div class="container">
      <div class="section-header">
        <p>The control head is the electronic brain of a water softener — it tracks water use, triggers regeneration, and manages the internal piston that redirects water flow. At {site.gpgLow}-{site.gpgHigh} GPG, per {site.waterAuthority}, a {site.city} softener's control head cycles more often than in a softer-water market, which shortens its working life compared to a city with lower hardness.</p>
      </div>

      <h2>Why Control Heads Fail</h2>
      <p>{site.waterAuthority} data shows five symptoms point specifically to the control head rather than the resin bed or brine tank.</p>
      <div class="grid grid-3">
        {signs.map((s) => (
          <div class="card">
            <p class="card-title">{s.name}</p>
            <p>{s.detail}</p>
          </div>
        ))}
      </div>
      <p>If your symptoms point to the resin bed or brine tank instead, see our full <a href="/repair/">Water Softener Repair and Maintenance in Indianapolis</a> page.</p>
    </div>
  </section>

  <QuickFacts />

  <section class="section" style="background: var(--color-surface);">
    <div class="container">
      <div class="section-header center">
        <h2>Repair vs. Replacement</h2>
        <p>Two factors decide which makes sense for a given control head at {site.city}'s {site.gpgLow}-{site.gpgHigh} GPG hardness level.</p>
      </div>
      <div class="grid grid-2">
        <div class="card">
          <p class="card-title">Repair When...</p>
          <p>The issue is a programming drift, a sensor fault, or a single replaceable part on a head under 10 years old — most control head calls fall into this category.</p>
        </div>
        <div class="card">
          <p class="card-title">Replace When...</p>
          <p>The circuit board or motor has failed on a discontinued model, or the head is original to a system over 12 years old and parts availability is limited.</p>
        </div>
      </div>
      <p>According to {site.waterAuthority}'s hardness data for {site.county}, most control heads in this market need attention between years 8 and 12 — get a free diagnostic to find out where yours stands.</p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="section-header">
        <h2>Control Head Repair Cost in {site.city}</h2>
        <p>Reprogramming or a sensor fix typically runs $75 to $150. A motor or circuit board replacement runs $150 to $350 in parts and labor, and a full head swap on an aging system runs $250 to $500. {site.waterAuthority} reports the same {site.gpgLow}-{site.gpgHigh} GPG figure behind every quote — always with a written quote before work begins, get a free quote to confirm your exact cost.</p>
      </div>
    </div>
  </section>

  <section class="section" style="background: var(--color-surface);">
    <div class="container">
      <div class="section-header center">
        <h2>Control Head Repair Questions</h2>
        <p>{site.waterAuthority} data shows three questions come up most on {site.city} control head calls.</p>
      </div>
      <div class="faq-list">
        {faqs.map((item) => (
          <details class="faq-item">
            <summary>
              {item.q}
              <svg class="faq-chevron" width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
                <path d="M4 6.5 8 10.5 12 6.5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            </summary>
            <p>{item.a}</p>
          </details>
        ))}
      </div>
      <p>{site.waterAuthority} reports the same {site.gpgLow}-{site.gpgHigh} GPG figure behind every answer above — contact us with any other questions.</p>
    </div>
  </section>

  <section class="section section-dark">
    <div class="container final-cta">
      <h2>Control Head Acting Up in {site.city}?</h2>
      <p>{site.waterAuthority} reports the same {site.gpgLow}-{site.gpgHigh} GPG figure behind every diagnostic — get a free in-home diagnostic with every repair call.</p>
      <a class="btn btn-primary" href="/contact/">Book a Repair Call →</a>
    </div>
  </section>

  <style>
    .card-title { font-family: var(--font-heading); font-size: 1.1rem; color: var(--surface-footer); margin: 0 0 8px; }
    .faq-list { max-width: 760px; margin: 24px auto 0; }
    .final-cta { text-align: center; }
    .final-cta p { max-width: 48ch; margin: 0 auto 24px; }
  </style>
</Layout>
